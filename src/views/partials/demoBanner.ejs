<% if (typeof isDemoMode !== 'undefined' && isDemoMode) { %>
  <div class="demo-mode-banner">
    <div class="container">
      <div class="demo-mode-banner-content">
        <div class="demo-mode-banner-icon">
          <i class="fa-solid fa-flask"></i>
        </div>
        <div class="demo-mode-banner-text">
          <strong>Demo Mode Active:</strong> All commands are simulated. No files can be written, and no actual containers will be affected.
        </div>
      </div>
    </div>
  </div>
<% } %>
